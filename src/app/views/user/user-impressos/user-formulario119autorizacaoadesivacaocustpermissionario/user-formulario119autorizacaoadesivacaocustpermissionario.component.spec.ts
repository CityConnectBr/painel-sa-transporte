/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { UserFormulario119autorizacaoadesivacaocustpermissionarioComponent } from './user-formulario119autorizacaoadesivacaocustpermissionario.component';

describe('UserFormulario119autorizacaoadesivacaocustpermissionarioComponent', () => {
  let component: UserFormulario119autorizacaoadesivacaocustpermissionarioComponent;
  let fixture: ComponentFixture<UserFormulario119autorizacaoadesivacaocustpermissionarioComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ UserFormulario119autorizacaoadesivacaocustpermissionarioComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UserFormulario119autorizacaoadesivacaocustpermissionarioComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
